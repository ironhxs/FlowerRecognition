# ConvNeXt Large Model Configuration
# Larger model for maximum accuracy (if size permits)

name: convnext_large
architecture: convnext_large
pretrained: true
num_classes: 100

# Model specific settings
drop_path_rate: 0.2
drop_rate: 0.0
layer_scale_init_value: 1e-6

# Input
input_size: 600
in_chans: 3

# Inference optimization
use_compile: false
