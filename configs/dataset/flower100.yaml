# Dataset Configuration for Flower Recognition

name: flower100
num_classes: 100

# Data paths
train_csv: ${data_dir}/train.csv
train_dir: ${data_dir}/train
test_dir: ${data_dir}/test

# Data split
val_split: 0.15  # 15% of training data for validation

# Image settings
image_size: 600
channels: 3

# DataLoader settings
batch_size: 32
shuffle: true
